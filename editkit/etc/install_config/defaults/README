This is the site-specific data directory *template*.  We copy the 'editkit'
directory to sys.prefix during a new installation, and all user/site-specific
data (uploads, site-specific settings) lives there.
